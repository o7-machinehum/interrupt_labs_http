{
  "name": "interrupt-labs-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "hugo --cleanDestinationDir --minify",
    "server": "hugo server --buildFuture --disableFastRender",
    "check": "hugo --cleanDestinationDir --minify"
  }
}
