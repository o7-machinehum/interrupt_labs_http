# Interrupt Labs Blog
Static site generated by Hugo.

## Getting Started
``` bash
hugo server --buildFuture --disableFastRender
```

## Generating Page
``` bash
hugo --cleanDestinationDir --minify
```

The generated site is written to `docs/` for GitHub Pages.

